## Tasks To Do

> When adding a transfer on the server add descriptions as To/From e.g. "To SIMCASH" or "From NEDBEE"
> Easiest way to edit transaction. Delete original and adjust the affected account. Add the edited one and adjust the affected account.
> Create a functionality for payables and receivables, so that they won't be enacted by account transfers. Split payables balance and on account view if account === payable/receivable aggregate the transactions by description(person name) and show it in an accordion with a balance in the accordion header
> add option to disable deleting of transactions older than 30 days
> Continue with the expense tracker project and create a commercial one with organization's and daily exchange rate. Query data by month and pagination your queries. The rates table will have day,month and year fields and an array of name,value rates
